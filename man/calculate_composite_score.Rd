% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/naming_utils.R
\name{calculate_composite_score}
\alias{calculate_composite_score}
\title{Calculate Composite Score for gRNAs}
\usage{
calculate_composite_score(
  gRNA_df,
  weights = base::c(gc = 0.25, offtarget = 0.35, tm = 0.1, position = 0.1, structure =
    0.2),
  optimal_gc = 50,
  optimal_tm = 60,
  optimal_position = 50
)
}
\arguments{
\item{gRNA_df}{A data frame containing gRNA data with columns: percentGC, Tm, n0, n1, position_percent.
Optionally includes palindrome_score and hairpin_score (from pre-filtering step).}

\item{weights}{A named numeric vector specifying the weight for each scoring component.
Default: c(gc = 0.25, offtarget = 0.35, tm = 0.10, position = 0.10, structure = 0.20).}

\item{optimal_gc}{Numeric. The optimal GC content percentage (default: 50).}

\item{optimal_tm}{Numeric. The optimal melting temperature in Celsius (default: 60).}

\item{optimal_position}{Numeric. The optimal position percentage in the target (default: 50).}
}
\value{
The input data frame with an added \code{composite_score} column (0-100 scale).
}
\description{
Calculates a composite quality score for each gRNA based on multiple criteria
including GC content, off-target specificity, melting temperature, position,
and sequence structure quality (palindrome/hairpin avoidance).
}
\examples{
\dontrun{
gRNA_df <- data.frame(
  protospacer = c("ATCG...", "GCTA..."),
  percentGC = c(48, 62),
  Tm = c(59, 65),
  n0 = c(1, 2),
  n1 = c(0, 1),
  position_percent = c(45, 75)
)
scored_df <- calculate_composite_score(gRNA_df)
}
}
