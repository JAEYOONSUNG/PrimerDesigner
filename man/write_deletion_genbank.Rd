% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_deletion_genbank.R
\name{write_deletion_genbank}
\alias{write_deletion_genbank}
\title{Write Deletion Vector GenBank File}
\usage{
write_deletion_genbank(
  vector_file,
  insert_seq,
  start,
  end,
  locus_tag,
  primers,
  upstream_bp,
  downstream_bp,
  output_path,
  kill_snapgene = FALSE
)
}
\arguments{
\item{vector_file}{Path to the original vector file (.dna, .gb, .fasta).}

\item{insert_seq}{Character string. Combined arm sequence (upstream + downstream).}

\item{start}{Integer. Vector insertion site start position (1-based, inclusive).}

\item{end}{Integer. Vector insertion site end position (1-based, inclusive).}

\item{locus_tag}{Character. Target locus tag for annotation labels.}

\item{primers}{Named list. Output from \code{design_deletion_primers()}.}

\item{upstream_bp}{Integer. Length of upstream arm.}

\item{downstream_bp}{Integer. Length of downstream arm.}

\item{output_path}{Character. Path for the output GenBank file.}

\item{kill_snapgene}{Logical. Kill SnapGene before reading .dna (default: FALSE).}
}
\value{
Invisible. The output file path.
}
\description{
Generates a modified GenBank file for a deletion vector construct.
Takes the original vector GenBank (converted from .dna if needed),
replaces the insertion site with upstream + downstream arms,
adjusts all existing feature locations, and adds annotations for:
arm regions (misc_feature) and 4 primers (primer feature with Tm info).
}
\details{
The output .gbk file can be opened directly in SnapGene to visualize
the deletion construct with all primer binding sites.
}
\examples{
\dontrun{
write_deletion_genbank(
  vector_file = "my_vector.dna",
  insert_seq = base::paste0(upstream_arm, downstream_arm),
  start = 1234, end = 1256,
  locus_tag = "QT234_RS00005",
  primers = primer_result,
  upstream_bp = 500, downstream_bp = 500,
  output_path = "QT234_RS00005_deletion_vector.gbk"
)
}
}
