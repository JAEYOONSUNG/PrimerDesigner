% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_cloning_primers.R
\name{batch_deletion_primers}
\alias{batch_deletion_primers}
\title{Batch Design Deletion Primers for Multiple Locus Tags}
\usage{
batch_deletion_primers(
  vector_file,
  genome_seq = NULL,
  genbank_table = NULL,
  locus_tags,
  start,
  end,
  upstream_bp = 500,
  downstream_bp = 500,
  tm_target = 60,
  overlap_length = 16,
  output_file = NULL,
  output_dir = NULL,
  genbank_file = NULL,
  kill_snapgene = TRUE
)
}
\arguments{
\item{vector_file}{Path to the vector file (.dna, .gb, .fasta).}

\item{genome_seq}{Character string of the full genome sequence.
Not required if \code{genbank_file} is provided.}

\item{genbank_table}{Data frame with columns: \code{locus_tag}, \code{start}, \code{end}.
Optionally: \code{gene}, \code{product}. Not required if \code{genbank_file} is provided.}

\item{locus_tags}{Character vector of locus tags to process. Use "all" for all tags.}

\item{start}{Integer. Vector insertion site start (1-based).}

\item{end}{Integer. Vector insertion site end (1-based).}

\item{upstream_bp}{Integer. Upstream arm length in bp (default: 500).}

\item{downstream_bp}{Integer. Downstream arm length in bp (default: 500).}

\item{tm_target}{Numeric. Target Tm (default: 60).}

\item{overlap_length}{Integer. Initial vector overlap (default: 16).}

\item{output_file}{Character. Path to save Excel output (default: NULL).}

\item{output_dir}{Character. Directory to save GenBank files per locus tag (default: NULL).
Each locus tag gets a \code{{tag}_deletion_vector.gbk} file with primer annotations.}

\item{genbank_file}{Character string. Path to a genome GenBank file (.gb, .gbk, .gbff).
When provided, \code{genome_seq} and \code{genbank_table} are automatically extracted
from this file. This overrides any separately provided \code{genome_seq} and \code{genbank_table}.}

\item{kill_snapgene}{Logical. Kill SnapGene before reading .dna files (default: FALSE).}
}
\value{
A data frame with one row per locus tag, containing all 4 primer
sequences, Tm values, and metadata.
}
\description{
Wrapper that calls \code{design_deletion_primers} for each locus tag and
returns a combined data frame. Extracts upstream/downstream arms from the
genome sequence based on gene coordinates in a genbank table.
}
\examples{
\dontrun{
# With separate genome_seq + genbank_table
result_df <- batch_deletion_primers(
  vector_file = "my_vector.dna",
  genome_seq = genome_sequence,
  genbank_table = genbank_df,
  locus_tags = c("QT234_RS00005", "QT234_RS00010"),
  start = 1234, end = 1256,
  output_file = "deletion_primers.xlsx",
  output_dir = "deletion_results",
  kill_snapgene = TRUE
)

# With genome GenBank file (auto-extract genome_seq + genbank_table)
result_df <- batch_deletion_primers(
  vector_file = "my_vector.dna",
  genbank_file = "my_genome.gbk",
  locus_tags = c("QT234_RS00005", "QT234_RS00010"),
  start = 1234, end = 1256,
  output_file = "deletion_primers.xlsx",
  output_dir = "deletion_results"
)
}
}
